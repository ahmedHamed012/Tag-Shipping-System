BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[shipment_items] ADD [price] DECIMAL(10,2) NOT NULL CONSTRAINT [shipment_items_price_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
