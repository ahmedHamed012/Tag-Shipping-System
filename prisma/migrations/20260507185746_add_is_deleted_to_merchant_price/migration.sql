BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[merchant_prices] ADD [isDeleted] BIT NOT NULL CONSTRAINT [merchant_prices_isDeleted_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
